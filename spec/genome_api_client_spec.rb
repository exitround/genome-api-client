require 'spec_helper'

describe GenomeApiClient do
  it 'has a version number' do
    expect(GenomeApiClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
